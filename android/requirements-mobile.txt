# Python dependencies bundled into the Android APK.
#
# Deliberately excludes the desktop-only native packages that cannot be built
# for Android and are not used by the mobile ShotSync selection profile:
#   - rawpy (LibRaw)      : RAW decoding, unused (server delivers JPEG previews)
#   - onnxruntime         : AI (faces / CLIP), disabled on mobile
#   - pyinstaller         : desktop packaging only
#   - send2trash          : desktop filesystem trash, unused
#   - pyobjc-*            : macOS only
#
# PySide6 / shiboken6 are supplied to pyside6-android-deploy as prebuilt
# Android wheels (see android/README.md), not via this file.
numpy
pillow
